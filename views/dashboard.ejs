<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Survey Portal - Dashboard</title>
</head>
<body>
  <h1>Dashboard</h1>
  <p>Welcome, <%= username %>!</p>

  <!-- Survey List -->
  <h2>Survey List</h2>
  <ul id="surveyList">
    <% surveys.forEach(survey => { %>
      <li>
        <strong>Survey ID:</strong> <%= survey.SurveyId %><br>
        <strong>Name:</strong> <%= survey.Name %><br>
        <strong>CPI:</strong> <%= survey.CPI %><br>
        <strong>LOI:</strong> <%= survey.LOI %><br>
        <strong>IR:</strong> <%= survey.IR %><br>
        <!-- Add more survey details as needed -->
      </li>
      <br>
    <% }); %>
  </ul>
</body>
</html>
